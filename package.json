{
  "name": "campus-operating-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build":"vite build",
    "dev": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\""
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "concurrently": "^9.2.1"
  }
}
